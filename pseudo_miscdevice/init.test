#!/bin/sh
# 1. Mount essential pseudo-filesystems
mount -t proc none /proc
mount -t sysfs none /sys

# 2. Mount devtmpfs so the kernel automatically populates device nodes (like ttyAMA0)
mount -t devtmpfs devtmpfs /dev

echo -e "\n=========================================="
echo -e "   Successfully Booted QEMU ARM64 Target   "
echo -e "==========================================\n"

# 3. Load the miscdevice driver and run its test
echo "Loading pseudo_misc.ko..."
insmod /pseudo_misc.ko
if [ -f /test_pseudo_misc ]; then
    chmod +x /test_pseudo_misc
    /test_pseudo_misc
fi

dmesg | grep -E "pseudomisc"

poweroff -f
